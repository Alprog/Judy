
varying vec2 v_texCoord;
uniform sampler2D mainTexture;
varying vec3 pos;

void main()
{	
	gl_FragColor = texture2D(mainTexture, v_texCoord);
	//gl_FragColor.rgb = vec3(pos.z, -pos.z, 0);
}
