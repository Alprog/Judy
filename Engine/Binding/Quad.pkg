$#include "Quad.h"

class Quad : public Node
{

public:
    Quad();

    virtual void Update(double delta);
    virtual void Render(Renderer* renderer);

    Vector2 Size;
    std::string Shader;
    std::string Texture;
};
